# Formatting for the crate; edition tracks Cargo.toml (2021, not 2024) until we migrate.
edition = "2021"
max_width = 100
use_small_heuristics = "Max"

# unstable options (nightly rustfmt only):
# comment_width = 100
# group_imports = "StdExternalCrate"
# format_strings = true
# wrap_comments = true
