/target/
# nginx-autoconf build output directories (one per BUILD flavor, see Makefile)
/objs-*/
# Test-harness OTel collector file-exporter output (generated at runtime)
/test-harness/logs/*.json
/test-harness/logs/*.json.*
# Claude Code project workspace — local only, not part of source history
/.claude/
# ASan run-log directories (timestamped, generated at runtime)
/tests/asan-logs-*/
